<%if current_author.voted_for?(post)%>
    <%= link_to "unlike", like_post_path(post), method: :put, remote: true %>
<%else%>
    <%= link_to "like", like_post_path(post), method: :put, remote: true %>
<%end%>